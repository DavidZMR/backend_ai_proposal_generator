# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# LLM — Anthropic Claude (tool_use)
anthropic>=0.25.0

# Transcripción — OpenAI Whisper
openai>=1.0.0

# Procesamiento de documentos
python-docx>=1.1.0
pdfplumber>=0.11.0

# Generación de PDF por código (sin dependencias externas)
reportlab>=4.0.0

# RAG — Embeddings + Vector DB
sentence-transformers>=3.0.0
chromadb>=0.5.0

# Persistencia — Supabase
supabase>=2.0.0

# Config
python-dotenv>=1.0.0
